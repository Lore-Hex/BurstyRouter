# Task: BurstyRouter Increment C — Prometheus /metrics + single-file /ui savings odometer

On the committed tree (has the savings meter: /stats exposes requests_total,
in_flight_local, forced_local/forced_tr, bursts_full/bursts_error/
bursts_skipped_unmapped, cloud_mode, cloud_spend_usd, cloud_blocked_* and a
`savings` object {saved_usd, local_tokens_prompt, local_tokens_completion,
cloud_spend_usd, usage_unknown_total, references:{model:count}, since}).
READ proxy.go's stats snapshot for the EXACT field names before wiring.
Do NOT commit. Do not touch `.codex-*`.

## 1. GET /metrics — Prometheus text (no client library, zero deps)
Emit the counters with # HELP/# TYPE: bursty_requests_total,
bursty_in_flight_local (gauge), bursty_route_total{route=...},
bursty_bursts_total{reason="full|error|skipped_unmapped"},
bursty_saved_usd_total, bursty_cloud_spend_usd_total,
bursty_local_tokens_total{kind="prompt|completion"}, bursty_usage_unknown_total,
bursty_cloud_blocked_total{reason="budget|mode"}. Token-gated when -token is
set (like /stats). Ship docs/grafana-dashboard.json (savings stat panel,
local-vs-cloud rate, in-flight gauge, cloud spend). Test: format parses, key
lines + values match a seeded snapshot.

## 2. GET /ui — one go:embed HTML file (internal/proxy/ui.html), no build step
Bearer-gated like /stats. NO framework, NO external assets (inline CSS+JS,
CSP-safe, offline). Polls /stats every 2s (same-origin fetch; if -token set,
prompt once, send Bearer). Renders: a BIG dollar odometer of cumulative
saved_usd (the hero number) with "since <date>" + reference model(s);
local-vs-cloud split (counts + bar, % local); in-flight-vs-capacity gauge;
cloud spend + current -cloud mode + budget remaining if capped; a live feed
of the last ~20 routing decisions (route+reason). Add a bounded ring buffer
(cap 100) of recent decisions to the server next to the stats counters,
exposed token-gated (a /stats `recent` field or tiny /ui/feed). READ-ONLY
status page (footer says so). HTML < 15KB.

## Wiring + tests
ServeHTTP: register /metrics, /ui (+ feed) with their own token gate before
the /v1 wall (only /healthz open). README "Dashboards" section; SETUP/skill
mention /ui as the "it's working" screen. Tests: /metrics format + token
gate; /ui served + gate + contains the odometer element id; ring buffer
bounded+populated; e2e (binary) hits /ui and /metrics.

## Gates
gofmt -l . (empty) · go vet ./... · go build ./... · go test -race ./... ·
~/go/bin/staticcheck ./... (/private/tmp cache envs; socket e2e self-skips in
sandbox, verified outside).
