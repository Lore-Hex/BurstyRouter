# Task: BurstyRouter Increment D — zero-config, packaging, boot banner

On the committed tree. Do NOT commit. Do not touch `.codex-*`.

## 1. Local-server autodetect
When `-local-url`/BURSTY_LOCAL_URL is unset, before proxy.New in
cmd/burstyrouter: probe (GET /v1/models, 750ms timeout each, in order)
`$OLLAMA_HOST` (if set, normalized), `127.0.0.1:11434` (Ollama),
`127.0.0.1:1234` (LM Studio), `127.0.0.1:8080` (llama.cpp server),
`127.0.0.1:8000` (vLLM). First responder wins; log which was found and how
many models it serves. Nothing found + no TR key → exit with a friendly
actionable error; nothing found + TR key → pure passthrough mode (say so).
`-no-autodetect` disables.

## 2. Boot banner (screenshotable)
On successful start print a compact banner (plain ASCII, no color deps):
name+version, detected/configured local upstream (server flavor guess +
model count), cloud upstream (host or "disabled"), mode/caps if set, and
the one line that matters: `Point your tools at http://localhost:8383/v1`.
Include the savings totals line when state has history (loaded from
Increment B's state file). Keep it ≤10 lines.

## 3. Packaging
- `.goreleaser.yaml`: builds for darwin/{arm64,amd64}, linux/{arm64,amd64},
  windows/amd64; archives with README+LICENSE; checksums; a `brews:` section
  publishing to tap repo `Lore-Hex/homebrew-tap` (formula `burstyrouter`);
  release notes from the tag. Add `.github/workflows/release.yml` running
  goreleaser on `v*` tags (permissions: contents write; tap push via a
  `HOMEBREW_TAP_GITHUB_TOKEN` secret — document in RELEASING.md that the
  secret must exist, with a graceful skip if absent if goreleaser supports
  it, else document-only).
- `Dockerfile` (multi-stage, distroless/static, ~10MB) + a compose example
  in docs/SETUP.md (ollama + bursty pair). `.dockerignore`.
- `RELEASING.md`: tag-driven release flow, tap secret note, Docker build.
- README quickstart becomes: brew install one-liner (with go install +
  releases-page + docker as alternates); SKILL.md/SETUP.md install steps
  updated to prefer brew/binary.
- Version: embed via `-ldflags -X main.version=` in goreleaser; `-version`
  flag prints it (default "dev").

## Tests
Autodetect: unit tests with httptest servers on injected probe lists (found/
not-found/ordering/timeout); banner golden test (string contains the key
lines); `-no-autodetect`; exit-path behavior via config validation tests.
`bash -n` any scripts; goreleaser config validated with `goreleaser check`
if the binary exists locally, else skip with a note (do NOT install it).

## Gates
gofmt -l . (empty) · go vet ./... · go build ./... · go test -race ./... ·
~/go/bin/staticcheck ./... (/private/tmp cache envs).
